# AI 开发规则（每次编码前必读）

> 本项目是一个**毕业论文配套系统**：在线 Markdown 笔记编辑与管理平台。
> 最终目标：一个可演示、可测试、功能与论文大纲严丝合缝的系统 + 一篇约 1 万字的毕业论文。
> **任何 AI agent 在本仓库工作，都必须遵守本文件。读完再动手。**

---

## 1. 强制流程：编码前 / 编码后

### 1.1 每次会话开始（编码前），按顺序完成：

1. **读本文件**（AGENTS.md）。
2. **读记忆三件套**：
   - `docs/memory/PROGRESS.md` —— 当前进度（做到大纲哪一节了）
   - `docs/memory/DECISIONS.md` —— 已有的技术决策，不许推翻
   - `docs/memory/CHANGELOG.md` —— 最近改了什么，避免重复劳动
3. **读 `docs/THESIS_OUTLINE.md`** 中与本次任务对应的章节，明确：本次写的代码将来要支撑论文的**哪几个小节**。
4. 向用户输出一行进度简报（例：`当前进度：5.2 用户认证模块已完成，本次任务：5.3 笔记管理`），然后才开始干活。

### 1.2 每次编码后（收工前），必须：

1. **更新 `CHANGELOG.md`**：时间倒序追加一条，格式见 §5。
2. **更新 `PROGRESS.md`**：把完成的大纲小节状态改为 ✅，新发现的待办记入。
3. **有选型/架构决策 → 更新 `DECISIONS.md`**：编号追加，格式见 §5。
4. **产出论文素材**（见 §6）：截图、测试表、接口文档等，与代码同步。

> ⚠️ 只写代码不更新记忆 = 本次工作未完成。不更新的行为视为违规。

---

## 2. 技术栈约束：核心锁定，细节放开

### 2.1 锁定项（不可更换，论文已按此写死）

| 层 | 技术 |
|---|---|
| 前端框架 | React 18 + TypeScript |
| UI 组件库 | Ant Design |
| 后端框架 | NestJS |
| 数据库 | PostgreSQL（笔记正文使用 JSONB） |
| 缓存/会话 | Redis |
| 实时协作 | Yjs（CRDT）+ WebSocket |
| 认证 | JWT |
| 部署 | Docker + Docker Compose |

- 这些技术**必须出现在最终系统里**，因为论文第 2 章逐一介绍了它们，实现与论文必须一致。
- 不许用同类库替换（如：不许用 Vue/Fastify/MongoDB/socket.io 自建同步协议等）。

### 2.2 待定项（agent 可决定，但必须记入 DECISIONS.md）

- **Markdown 编辑器**：Tiptap 与 Vditor 二选一，选定后写清理由（论文 2.1.3 写选型对比，实现必须用选中的那个）。**禁止两个都装。**
- **ORM**：推荐 TypeORM 或 Prisma，二选一，记入决策。
- **monorepo 组织**：推荐 pnpm workspace（`apps/web` + `apps/server`），可自行决定但需记录。
- 其他小型工具库（如 dayjs、bcrypt、class-validator）可以直接用。

### 2.3 依赖新增规则

- 每新增一个 package，必须在 CHANGELOG 里记一行**为什么加**。
- **禁止引入与已有依赖功能重叠的库**（不许既装 axios 又装 fetch 封装库、既装 Antd 又装 MUI）。
- 状态管理、CSS 方案等"写法级"选择：轻量优先（React 内置 + Antd 即可），确需引入时记入 DECISIONS。

---

## 3. 功能范围 = 论文大纲（禁止擅自增删）

- **允许实现的功能**：仅限 `docs/THESIS_OUTLINE.md` 中第 3.3 节列出的七个模块，外加 5.8 导出、5.9 部署。逐条对照，不许漏。
- **禁止私自加功能**：AI 辅助、白板、插件系统、多人语音、富文本所见即所得之外的花活——大纲没有的都不做。
- **禁止私自砍功能**：若某个大纲功能实现代价远超预期（如 PDF 导出），**停下来向用户说明并商量**，不许默默删掉或用空壳糊弄。
- 大纲 4.1.3 "微服务模块划分"：实际实现采用 **NestJS 模块化单体**（Module 划分），不做真正的微服务拆分；论文按"模块化设计"表述。agent 不要脑补成微服务架构。
- 用户角色按大纲 3.2 实现：个人用户 / 团队管理员 / 团队成员 / 访客（分享链接），四种视角都要可演示。

---

## 4. 工程基线：够用就好，但要能跑

- 项目目标是**论文 + 可现场演示的系统**，不是生产级 SaaS。禁止过度工程化：
  - 不做 k8s、CI/CD、监控告警、灰度发布等论文不需要的东西；
  - 不追求 100% 测试覆盖率，但每个模块要有**可复现的功能验证步骤**（第 6 章测试表的数据来源）；
  - 代码注释适度，**核心逻辑处写清"这段对应论文 X.X 节"**，方便后期摘录。
- **一键启动是硬指标**：`docker compose up` 必须能拉起前端、后端、PostgreSQL、Redis，并附 `README.md` 启动说明。
- 数据库建表 SQL 最终导出到 `docs/appendix-ddl.sql`（论文附录 A 直接用）。
- Docker Compose 配置最终确认后复制到 `docs/appendix-compose.yml`（论文附录 B）。
- 接口写完同步维护 `docs/api.md`（按大纲 4.6 分组：认证/笔记/团队/WebSocket 事件）（论文附录 C）。
- 遇到大纲没覆盖、规则没约定的决策：**先问用户，不要自作主张**。

---

## 5. 记忆三件套规范

所有记忆文件在 `docs/memory/` 下，均为**追加式、时间倒序**（最新在文件顶部）。

### 5.1 PROGRESS.md —— 进度账本

用大纲第 5 章小节做 checklist，状态标记：`⬜ 未开始 / 🟨 进行中 / ✅ 完成（含素材）`。
每次收工更新对应条目，并在文件顶部"当前状态"一句话栏写明：现在做到哪、下一步是什么、有什么阻塞。

### 5.2 DECISIONS.md —— 决策记录

编号格式 `D-001`、`D-002`…，每条包含：

```
## D-00X：<决策标题>
- 日期：
- 背景：为什么需要决策
- 选项：考虑过的方案
- 决定：选了什么
- 理由：为什么选它（这段以后可直接进论文）
- 影响论文小节：如 2.1.3 / 4.4.2
```

规则：已记录的决策不许无故推翻；要推翻，新增一条决策说明原因，不许删旧条目。

### 5.3 CHANGELOG.md —— 变更流水

时间倒序，每次会话至少一条：

```
## [YYYY-MM-DD] <会话主题>
- 做了什么：逐条列出（新增/修改了哪些模块、文件）
- 为什么：动机、对应大纲小节
- 新增依赖：无 或 列出并说明理由
- 产出素材：截图/测试表/文档（路径）
- 遗留问题：无 或 列出
```

> 诚实原则：失败了、没做完、绕了弯路，都要如实记录。记忆是给下一个 agent（和写论文的你）看的，撒谎或省略会直接坑害后续工作。

---

## 6. 论文素材同步产出（与代码同步，不许欠账）

论文约 1 万字，第 4、5 章每个小节至少 1 张图，第 6 章靠测试表撑起工作量。因此：

| 素材 | 要求 | 存放位置 |
|---|---|---|
| 界面截图 | 每完成一个界面，立即截图，命名 `<小节号>-<名称>.png`，如 `5.2.1-login.png` | `docs/assets/` |
| 架构/流程/ER 图数据 | agent 负责提供图的**文字底稿**（模块关系、表字段、流程步骤），用户负责画图 | `docs/diagrams/` |
| 功能测试用例表 | 每个模块完成后写用例表：编号/前置条件/操作步骤/预期/实际/结论（Markdown 表格） | `docs/testing/` |
| 性能测试数据 | 部署后协助设计压测方案与记录结果（6.3 节） | `docs/testing/perf.md` |
| 接口文档 | 见 §4 | `docs/api.md` |
| 数据库 DDL | 见 §4 | `docs/appendix-ddl.sql` |
| 论文可引用代码片段 | 核心逻辑文件在 PROGRESS.md 中标注"论文可引用：xxx.ts" | — |

**截图注意**：截图必须是真实运行的系统界面，不许用 Figma 假图或手绘替代。

---

## 7. 禁止事项清单

1. ❌ 不读记忆文件就开始编码。
2. ❌ 写完代码不更新三件套。
3. ❌ 更换/绕过 §2.1 锁定技术栈。
4. ❌ 增删大纲之外/之内的功能（除非用户明确同意）。
5. ❌ 修改 `docs/THESIS_OUTLINE.md`（大纲是用户定的，冻结）。
6. ❌ 用 TODO/占位符/假数据糊弄并标记"完成"。
7. ❌ 无记录的大规模重构。
8. ❌ 在记忆中谎报进度或省略失败。
9. ❌ 引入与现有依赖重叠的库。
10. ❌ 大纲未覆盖的重要决策不问用户、自作主张。

---

## 8. 收工自查清单（每次会话结束前逐条确认）

- [ ] 代码可运行？`docker compose up` 后核心流程走通？
- [ ] CHANGELOG.md 已追加本次记录？
- [ ] PROGRESS.md 状态已更新、"当前状态"一句话已改？
- [ ] 有决策 → DECISIONS.md 已编号记录？
- [ ] 本模块截图已存 `docs/assets/`？
- [ ] 本模块测试用例表已写 `docs/testing/`？
- [ ] 新增依赖都有理由记录？
- [ ] 论文对应小节已标注（代码注释 + PROGRESS）？
